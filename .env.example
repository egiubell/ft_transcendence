# Environment Configuration Template
APP_ENV=development
APP_PORT=8080
APP_NAME=PongTournament

# Security Settings (change these values)
APP_SECRET=your-secret-key-here
JWT_SECRET=your-jwt-secret-here

# Database Configuration (when implementing)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=transcendence
DB_USER=ponguser
DB_PASS=secure-password

# Add your actual values to .env file (not tracked by git)